- hosts: avant
  become: True
  tasks:
    - name: Install all the packages
      yum:
        name: "{{ item }}"
        state: present
        update_cache: True
      loop:
        - python3
        - python3-setuptools
        - nginx
        - php
        - python3-pip
        - gcc
